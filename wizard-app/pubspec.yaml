name: appwizard
description: "Bargain Wiz"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  
  # BLoC for state management
  flutter_bloc: ^8.1.6
  bloc: ^8.1.4
  equatable: ^2.0.5
  
  # Functional programming for error handling
  dartz: ^0.10.1
  
  # Dependency injection
  get_it: ^7.7.0
  
  # Internationalization
  intl: ^0.20.2
  
  # Network (for future use)
  dio: ^5.4.1
  
  # Local storage (for future use)
  shared_preferences: ^2.2.2
  
  # Network connectivity
  connectivity_plus: ^6.0.5
  
  # Firebase
  firebase_core: ^3.6.0
  firebase_analytics: ^11.3.3
  firebase_crashlytics: ^4.1.3
  firebase_messaging: ^15.1.3
  firebase_remote_config: ^5.1.4
  firebase_auth: ^5.3.3
  
  # Authentication
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.3
  
  # Routing
  go_router: ^14.6.1
  
  # Lottie animations
  lottie: ^3.1.2
  font_awesome_flutter: ^10.7.0
  
  # JSON serialization
  json_annotation: ^4.9.0
  flutter_svg: ^2.0.10+1
  
  # HTML parsing for rich text
  flutter_html: ^3.0.0-beta.2
  
  # URL launcher for opening external links
  url_launcher: ^6.3.1
  
  # Share functionality
  share_plus: ^10.1.2
  
  # In-app purchases
  in_app_purchase: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  
  # Native splash screen generator
  flutter_native_splash: ^2.4.1
  
  # Testing
  mockito: ^5.4.4
  build_runner: ^2.4.9
  json_serializable: ^6.8.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/config/
    - assets/lottie/
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Flutter Native Splash configuration
flutter_native_splash:
  # This package generates native code to customize Flutter's default white native splash screen
  # with background color and splash image.
  # Customize the parameters below, and run the following command in the terminal:
  # flutter pub run flutter_native_splash:create
  # To restore Flutter's default white splash screen, run the following command in the terminal:
  # flutter pub run flutter_native_splash:remove

  # Color or image to be used as background
  # You can use hex format: #ffffff or named colors: Colors.white
  color: "#ffffff"
  # color_dark: "#000000"  # Uncomment to use a dark mode background color
  
  # Optional: Specify a color for the status bar
  # status_bar_color: "#ffffff"
  # status_bar_color_dark: "#000000"  # Uncomment for dark mode
  
  # Optional: Specify an image to use as splash screen
  # image: assets/splash_logo.png
  # image_dark: assets/splash_logo_dark.png  # Uncomment for dark mode image
  
  # Optional: Specify a color for Android 12+ splash screen background
  # android_12: true
  # android_12_color: "#ffffff"
  # android_12_color_dark: "#000000"
  
  # Optional: Full screen mode (Android only)
  # fullscreen: true
  
  # Optional: Set to false to disable android, ios, or web
  # android: true
  # ios: true
  # web: true
  
  # Optional: Set to true if you want to keep the splash screen visible until Flutter is fully initialized
  # android_disable_fullscreen: false
  
  # Optional: iOS content mode
  # ios_content_mode: center  # Options: center, scaleAspectFill, scaleAspectFit

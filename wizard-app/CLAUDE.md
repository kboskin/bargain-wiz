# wizard-app

Flutter client (BLoC + GetIt + `dartz` + go_router + Firebase, run through `fvm`).
Stack, toolchain, flavors, layout and the things that bite are in AGENTS.md, imported below;
repo-wide conventions are in `../AGENTS.md`.

@AGENTS.md

# wizard-backend

Firebase project: Cloud Functions 2nd gen in Python 3.12 (pydantic v2, `google-genai` → Gemini
on Vertex AI, Cloud Tasks), Firestore rules and indexes, Storage rules. Stack, module map and
the rules not to break are in AGENTS.md, imported below; endpoint-by-endpoint reference is
`functions/README.md`.

@AGENTS.md
